import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const SignUp = () => {
    return (
        <SafeAreaView>
            <Text className="text-orange-500">Signup Screen</Text>
        </SafeAreaView>
    )
}

export default SignUp