import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const SignIn = () => {
    return (
        <SafeAreaView>
            <Text className="text-orange-500">Sign in Screen</Text>
        </SafeAreaView>
    )
}

export default SignIn