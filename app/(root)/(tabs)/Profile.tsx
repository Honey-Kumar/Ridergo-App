import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const Profile = ()=>{
    return(
        <SafeAreaView>
            <Text className="text-yellow-500">Profile</Text>
        </SafeAreaView>
    )
}

export default Profile