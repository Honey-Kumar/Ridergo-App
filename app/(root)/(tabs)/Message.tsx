import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const Message = () => {
    return (
        <SafeAreaView>
            <Text className="text-yellow-500">Message</Text>
        </SafeAreaView>
    )
}

export default Message