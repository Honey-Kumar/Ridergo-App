import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const Rides = () => {
    return (
        <SafeAreaView>
            <Text className="text-purple-600">Rides</Text>
        </SafeAreaView>
    )
}

export default Rides